export interface RadioGroupProps {
  /**
   * Shared `name` attribute for all child `<Radio>` inputs.
   * Auto-generated when omitted.
   */
  name?: string

  /**
   * Marks all radios in the group as required.
   */
  required?: boolean
}
